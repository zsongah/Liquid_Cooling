# AIDC 液冷智控软件：产品与使用手册

**软件 v0.4.0 · 配置 schema_version="0.1" · 核对日期：2026-09-14。** 本文是唯一维护的完整说明，覆盖产品定位、配置、算法、设备接入、仿真验证和代码职责。README 只保留快速入口；HTML 由本文生成。

当前交付是部署于边缘计算机的 CDU 监督控制工程验证版。已经实现 Modbus TCP 读写、流量/压差控制、数据记录、有界水力参数校准，并用实际 Sustain-LC FMU 完成闭环。**尚未连接厂家真实 CDU；本次 FMU 实验没有取得节能收益。** 可用于开发和协议联调，距离现场产品交付仍有明确工作。

## 1. 先看什么、先运行什么

没有硬件时，按“合成热工仿真 → 本机 TCP 联调 → 实际 FMU 闭环”使用。前两项需要 Python 3.9+，核心无第三方运行依赖；当前使用 Unix 的 fcntl 锁，Windows 还需适配。FMU 需要单独的 PyFMI 环境，本机通过已有 Docker 镜像运行。

以下命令先进入项目目录；每次实验使用新的输出目录，避免混合实验、审计与模型。

```bash
cd "/Users/song/Files/AIDC/液冷/液冷产品/lc-control"

# 配置检查，不连接设备
python3 -m lc_control validate examples/thermal_liquid_to_liquid.json

# 合成热工对象：固定设定值 / 固定参数算法 / 在线校准算法
python3 -m lc_control simulate examples/thermal_liquid_to_liquid.json --output outputs/my-l2l-001
python3 -m lc_control simulate examples/thermal_liquid_to_air.json --output outputs/my-l2a-001

# 实际 TCP 报文交换，对端是本机模拟器
python3 tools/wire_demo.py

# 实际 FMU：Docker 需已启动，镜像需已存在
python3 tools/run_sustain_fmu.py --docker \
  --fmu /Users/song/Developer/sustain-lc/LC_Frontier_5Cabinet_4_17_25.fmu \
  --output outputs/my-fmu-001
```

| 想了解什么 | 阅读位置或已有结果 |
|---|---|
| 定位、功能与上下游 | 第 2 节；[产品全景图](assets/aidc-product-overview-v1.png) |
| 配置、拓扑、观测与控制映射 | 第 3–4 节；[现场模板](../examples/field_template.UNBOUND.json) |
| 算法、学习与外部功率预测 | 第 5–6 节 |
| 厂家适配和边缘部署 | 第 7–8 节 |
| FMU 使用及验证边界 | 第 9 节；[实际 FMU 报告](../outputs/sustain-fmu-closed-loop-v2/index.html) |
| 当前指标与后续目标 | 第 10、13 节 |
| 逐文件职责、故障恢复 | 第 11–12 节 |

## 2. 产品定位、模块和上下游

软件接收配置、设备观测和可选负荷预测，计算 CDU 监督层目标。CDU 本地 PLC/DDC 继续负责泵阀快速内环、联锁和失联接管。通用性来自统一数据语义、能力声明和独立适配器；不意味着任意品牌仅填 IP 就能控制。

```text
上游：设备点表 / 工程限值 / CDU—机柜—热汇关系 / 可选机柜功率预测
                                  ↓
边缘软件：接入归一 → 数据检查 → 热需求与目标计算 → 命令网关
                         ↑              ↓                 ↓
                         └── 参数校准 ← 数据和决策记录     目标下发
                                                          ↓
下游：CDU 本地控制器 → 泵阀与液冷回路 → 运行反馈 → 接入归一

外部 BMS / iCooling / 冷源群控：先明确控制权与协调接口
当前未实现这些平台的专用连接器或多 CDU / 冷站协调器
```

| 模块 | 已实现 | 当前边界 |
|---|---|---|
| 配置与拓扑 | 资产、服务机柜、热汇、属主、共享关系、能力与限值校验 | 不求解完整水路，不保证每个机柜流量 |
| 接入采集 | Modbus TCP、标准单位、时间与质量、告警、模式、属主、目标回读 | 无厂家实机验收；无 RTU、BACnet、SNMP、Redfish 驱动 |
| 控制 | 热需求计算、正温差反馈、流量或压差目标 | 自动策略保持供温，不直接调泵频，不做全站总能耗寻优 |
| 预测预警 | 一阶回液预测、软限值风险、容量不足、通信/数据故障记录 | 时间常数来自配置；无独立热负荷时可能退化为持值；不是芯片模型 |
| 校准 | 水力增益拟合、时间保留验证、有界晋升、模型恢复 | 只学习一个聚合水力参数，不学习安全限值 |
| 执行网关 | 运行模式、限幅限速、期限、预写审计、回读、暂停和释放 | 设备必须独立实现失联保护；不自动重新获取控制权 |
| 仿真与功耗 | 合成热模型、TCP 模拟器、CSV 回放、实际 FMU 闭环、电量比较 | 结果仅对相应模型和实验条件有效 |
| 运维输出 | CLI、SQLite、JSONL、静态 HTML 和实验曲线 | 无实时网页控制台、通知服务、多用户权限或发布审批平台 |

## 3. 配置与最小拓扑

可执行配置统一放在 `examples/`。软件 v0.4.0 与配置版本 "0.1" 是两个概念；不要将 schema_version 改为 "0.4"。配置经 `configuration.load_scene()` 校验后建立适配器。现场模板空地址及占位 IP 会故意导致校验失败，其余数值也只是示例。

| 配置位置 | 内容 | 对核心的影响 |
|---|---|---|
| 根字段 | schema_version、scene_id、topology_version、provenance、notes | 版本与来源；拓扑版本参与命令检查 |
| assets | 稳定 id、kind：cdu、rack、facility_water、room_air | 校验资产引用和排热路径 |
| control_domains | id、cdu_id、owner、heat_sink_id、served_racks、shared_hydraulics | 选择对象、归属、预测聚合范围及共享控制限制 |
| devices[id].type / adapter | liquid_to_liquid 或 liquid_to_air；mock、thermal_sim、modbus_tcp、sustain_fmu | 选择热汇检查和适配器；FMU 走专用实验入口 |
| connection / points | 网络、超时、标准量与设备点位绑定 | 通信、解码、单位及状态语义 |
| controls | unit、minimum、maximum、max_step、minimum_interval_s、allowed_modes | 可写能力与动作约束 |
| guards / max_data_age_s | 观测工程上下限、允许数据年龄 | 异常阻止优化，进入暂停/接管流程 |
| policy | 比热、流量范围、温差、回温软限值、水力先验、时域、校准开关 | 需求计算、预测和晋升条件 |
| write_enabled / commissioning | 点表/限值确认、唯一控制权、设备看门狗、本地接管、证据引用 | 真实控制门槛；配置勾选不是自动认证 |
| initial_setpoints / initial_observations / simulation | 仿真初值、对象参数 | 仅用于开发对象，不能冒充实测 |
| extensions | 厂家/固件、文档、工程标签等元数据 | 普通核心不自动执行；FMU 工具专门读取 sustain_fmu_experiment |

**需要机柜拓扑，第一阶段只需最小服务关系。** 当前每台 CDU 恰属一个控制域，CLI 每次选择一个域。served_racks 决定哪些机柜预测汇总到该 CDU；热汇标明设施水或房间空气；共享水路表明动作会影响邻机，需要统一协调。液对液/液对气类型不决定压差/流量控制，实际可写能力才决定。

普通现场网关在 shared_hydraulics=true 时拒绝单域自动控制。不能为绕过检查把共享回路填成独立。目前没有跨域冗余供液、支路压降求解或机柜最小流量分配；FMU 的受限实验例外见第 9 节。

拓扑、工质、设备或策略变更后应重查参数与边界。模型恢复绑定完整场景哈希，改变配置不直接继承旧模型。当前没有在线热更新配置界面。向 extensions 增加字段不会自动增加物理算法；影响控制的新字段必须加入校验和测试。

## 4. 观测量、控制量与设备点表

| 标准量 | 内部单位 | 来源与用途 |
|---|---|---|
| cdu.sec_supply_temp / cdu.sec_return_temp | K | 二次供回液实测温度，热需求与保护检查 |
| cdu.sec_flow | kg/s | 二次质量流量，控制和水力校准 |
| cdu.sec_dp | kPa | 实际供回差压；不能用目标值或单点绝压冒充 |
| cdu.liquid_load | W_th | 可选独立液冷热负荷；缺失时由流量/温差反算 |
| cdu.electric_power | W_e | 可选电功率；需明确泵、整机或其他计量边界 |
| cdu.sec_supply_temp_sp | degC | 供液目标；自动策略保持，不寻优 |
| cdu.dp_sp / cdu.sec_flow_sp | kPa / kg/s | 自动策略二选一输出的目标及回读 |
| cdu.pump_speed_rel | 1 | 契约支持，当前 FlowPolicy 不输出泵速 |
| status.mode / owner / alarm | 设备枚举/状态 | 实际模式、归属和告警；未知不视为正常 |
| status.sample_counter | 设备计数 | 可选源停更检测；无真实计数不能证明传感器刷新 |

点表分 observations、setpoints、status、commands。绑定提供零基 address、read_function、dtype、unit；按需提供 byte_order、word_order、scale、offset、invalid_raw。设定量另需 write_function、tolerance，可用 write_address 区分写地址；当前读写共用编码与换算。

实现 FC01/02 单个位读取、FC03/04 寄存器读取、FC06/16 寄存器写入，以及 16/32 位有符号/无符号整数、Float32、布尔位。未实现 FC05/15 线圈写入、位域读改写、多步提交握手或延迟确认状态机。异步更新目标的设备应扩展适配，不能仅扩大回读容差掩盖失败。

- OEM 可能使用 40001 或一基编号；确认表示方式后转换为 PDU 零基地址，不能一律减 40001。
- °C 温度观测用 offset=273.15 转 K；温度设定仍为 °C。
- L/min 流量按实际密度换算：kg/s = L/min × density_kg_m3 / 60000。工质比热和密度都要有依据，不自动假定为纯水。
- 网络新回包不等于 PLC/传感器新采样。源计数器检查停更，不能替代传感器校准。
- setpoint_confirmed 表示目标回读一致；实际温度、流量、压差响应需单独看观测。

只读配置允许 controls={}。无写点、模式/归属不明或告警不可用时，不能宣称控制就绪。`examples/modbus_emulator.json` 地址为虚构，不能用于硬件。

## 5. 控制与在线校准

当前是有界规则控制，先计算液冷热需求，再调流量或压差。没有深度学习控制器，也没有以总冷却能耗为目标的优化求解。

```text
热负荷 Q = 独立液冷热负荷（若有）
         或 cp × 实测质量流量 × (回液温度 − 供液温度)
需求热量 = max(当前热负荷, 有效外部预测峰值)
可用温差 = min(目标温差, 回温软限值 − 当前供温)
需求流量 = 需求热量 / (cp × 可用温差) + 正温差超差反馈

流量模式：目标 = 有界需求流量
压差模式：目标 = (有界需求流量 / 水力增益)²
再经设备范围、单次幅度、死区、最短间隔和命令网关检查
```

无效数据、告警、供温超过策略回温软限值等条件会阻止正常优化。需求超能力记录容量不足，不能通过不断增大目标保证超出设备容量的需求。

一阶回温预测使用配置 thermal_tau_s 与 horizon_s。如果负荷来自同一组当前温差/流量且无独立预测，可能退化为持值预测；低误差不能证明预测有效。

### 5.1 学习逻辑与参数恢复

只校准 flow ≈ gain × √dp 中的聚合增益。需要正常质量、无告警、准稳态、非执行器边界且有足够压差变化。缺实际压差、异常或明显瞬态时冻结/清理样本，不把目标当测量拟合。

最近最多保存 60 个合格样本，最后 8 个按时间保留验证，训练至少 12 个；至少积累 20 个后，按采样计数每 10 个尝试候选。候选在 gain_bounds 内、单次变化最多 ±2%、验证 MSE 改善超过 2% 才可晋升；adaptive_enabled=false 时仅记录候选。

现场先关闭自动晋升，积累数据再开放。滚动保留窗口可重叠，不替代独立长期验证。学习不会放宽 guards 或 controls。原始数据和晋升写入 SQLite；相同完整配置可恢复已发布参数，重启后拟合窗口重新积累。

**更准确不自动等于更省电或更快。** 直接流量模式不使用该增益换算目标；本轮 FMU 校准后泵电量增加。响应速度、温度和能耗应分别评价。

## 6. 外部机柜功率预测

`run ... --forecast-file forecast.json` 每周期读取文件，生产方用临时文件加原子重命名更新。当前是预测数据消费接口，未实现预测服务、调度连接器或网络 API。

下面是格式示例，运行时替换为当前 Unix 秒并覆盖有效时域；rack_id 必须匹配所选域 served_racks。

```json
{
  "issued_at": 1800000000,
  "valid_until": 1800000030,
  "unit": "W_e",
  "racks": [
    {"rack_id": "RACK_01", "liquid_fraction": 0.8,
     "samples": [{"at": 1800000000, "power_w": 50000},
                 {"at": 1800000030, "power_w": 60000}]},
    {"rack_id": "RACK_02", "liquid_fraction": 0.8,
     "samples": [{"at": 1800000000, "power_w": 50000},
                 {"at": 1800000030, "power_w": 60000}]}
  ]
}
```

要求机柜完整且唯一、时间网格一致；拒绝缺失、重复、过期、错位、错误单位和非有限值。机柜总电功率乘液冷分担比例后按 CDU 汇总。当前仅用预测峰值增加需求，不用低预测压低已观测热需求。

这里的提前冷却是提前增大流量/压差，没有主动降低供温或命令冷站预冷。收益取决于预测提前量、误差、电到热时延、分担比例与设备响应。OEM liquid_load 若仍由同一温差/流量计算，就不是独立前馈信息。

## 7. 厂家接口依据与现场适配

以下保留前期调研中直接相关的一手资料。它们不是本项目兼容认证；型号、固件、点表、写权限、交接方式需逐台核对。本次整理核对本地代码与实验，没有重做全网厂家调查。

| 对象与来源 | 与本产品相关的做法 | 当前结论 |
|---|---|---|
| [深度智控 DeepChip](https://www.deepctrls.com/products/deepchip) | 设备侧感知、本地推理、液冷闭环定位 | 借鉴边缘执行与数据闭环；没有其私有算法/OEM 通用写接口，不采用其营销数字作为本项目指标 |
| [华为 iCooling](https://www.huawei.com/en/huaweitech/publication/90/smart-cooling-data-centers) / [TMU](https://digitalpower.huawei.com/ph/data-center-facility/tmu) | 上层优化平台与液冷设备是不同对象 | 未获得目标型号完整可写点表；iCooling 不是通用 CDU API |
| [英维克插框 CDU](https://www.envicool.com/productinfo544/0.html) | 列出 Modbus/SNMP 监控 | 无本项目完整写点表/交接验证，不能声称已适配 |
| [Vertiv XDU450 手册 Rev B，2022](https://www.vertiv.com/48e9f4/globalassets/shared/70616-xdu450-application-and-planning-guide-rev-b_7-22-final-locked.pdf) | 附录公开 Modbus 状态/告警/输入寄存器 | FC04 中温度目标属于读取项；远程停机线圈不是释放到本地自动命令；不外推到其他型号 |
| [施耐德液冷](https://www.se.com/au/en/work/products/product-launch/liquid-cooling-products/) / [Motivair 通信](https://www.motivaircorp.com/products/chilleddoor/remote-communication/) | PLC/BMS 集成，Modbus、BACnet IP/MS/TP、Lon 等 | 仍需对应型号点表，不能假定全系列统一写接口 |
| [Midea CDU](https://hvac.midea.com/products/datacentersolutions/cdu/833885013143621.shtml) | Modbus RTU/TCP、Redfish、BACnet | 本项目仅实现 Modbus TCP 子集 |
| [CoolIT CHx2000](https://www.coolitsystems.com/cdu-product/chx2000/) | Redfish 集成、群组控制 | 提示未来需资源发现/群控协调，尚无适配器 |
| [DMTF DSP2064 1.1.0](https://www.dmtf.org/sites/default/files/standards/documents/DSP2064_1.1.0.pdf) / [Modbus 1.1b3](https://www.modbus.org/file/secure/modbusprotocolspecification.pdf) | 液冷资源语义与协议访问定义 | 标准不提供厂家点表，不保证全部属性可写 |

以太网/RS-485 是连接层，Modbus/BACnet/Redfish 等是协议层，地址/单位/交接是设备语义层。三层对齐才能复用算法。串口网关还需确认 Unit ID、帧转换、超时，不能默认普通透传兼容。

### 7.1 适配包和联调顺序

适配包对应厂家、型号、控制器/固件和通信文档版本。只变地址、编码、单位和限值可改配置；协议、握手、回退语义变化需要扩展适配器并测试。

1. 确认对象是 CDU、群控 PLC 还是平台，明确目标属主；避免与 iCooling/BMS 同时写同一目标。
2. 从 `examples/field_template.UNBOUND.json` 绑定真实点表、权限和限值，删除不能证实的可选点；保持 write_enabled=false、adaptive_enabled=false。
3. validate 后 monitor，逐点对照设备面板，核对量程、单位、功率边界、告警、模式、属主和源刷新。
4. 实测目标写入/回读/过程响应、手动优先、唯一控制权、本地接管、断网、进程退出和设备重启。
5. 保存 commissioning 证据，再 shadow 与小范围 control；当前 shadow 也检查真实可控模式/归属，只读本地模式使用 monitor。
6. 建立温度、动作、能耗基线，再逐步开放校准；一个型号联通不代表全品牌兼容。

供温写入还需 OEM 防凝露保护证据，或有效露点观测与余量。当前自动算法不调温，通用网关仍检查温度请求。缺少机柜关键温度/流量时，不能推断机柜热安全。

## 8. 运行模式与边缘部署

| 模式 | 行为 |
|---|---|
| monitor | 默认持续采集和记录，不写优化目标 |
| shadow | 生成、校验、记录候选，不写；检查真实模式与归属 |
| control | 满足能力、边界、数据与验收条件后下发目标 |
| paused | 故障内部状态，停止优化并尝试释放；不是 CLI 启动选项 |

### 8.1 本机 TCP 模拟器

```bash
# 终端一：虚构 CDU，仅本机验证
python3 -m lc_control emulator examples/modbus_emulator.json

# 终端二：只读
python3 -m lc_control run examples/modbus_monitor_only.json --steps 3 --output outputs/my-monitor-001

# 重启模拟器后，再执行控制演示
python3 -m lc_control run examples/modbus_emulator.json --mode control --steps 12 --output outputs/my-wire-001
```

模拟器失去心跳 15 秒后回本地，不自动重新授权；长时间监测后做控制需重启模拟器。现场按真实交接流程操作。`tools/wire_demo.py` 封装启动、四周期控制、退出检查，固定输出到 outputs/wire-demo。

### 8.2 Linux 现场部署示例

下列是目标边缘机路径示例，本地未安装服务、没有真实 site.json。先准备运行用户、虚拟环境、程序、核对后的配置及可写数据目录：

```bash
cd /opt/lc-control
.venv/bin/python -m lc_control validate /etc/lc-control/site.json
.venv/bin/python -m lc_control run /etc/lc-control/site.json \
  --mode monitor --steps 0 --interval 5 --output /var/lib/lc-control/site
```

--steps 0 持续运行；--domain 选择域；--forecast-file 传预测文件；--interval 设置循环周期。控制需显式选择。`deploy/lc-control-monitor.service` 是未安装、未在目标机验收的只读示例，不自动重启为控制。

客户端每次交换建立 TCP 连接，没有连接池/批量点读取；有交换超时与整轮扫描期限。按设备刷新与网络时延设置周期，不承诺大规模高频吞吐。host 使用 IP 字面地址。传统 Modbus TCP 无本驱动提供的认证/加密，配置开关也不是权限系统，应在受管理设备网络部署。

正常退出、Ctrl+C、CLI 收到 SIGTERM 均尝试释放。强杀/断电靠设备独立看门狗。同机端点锁不替代跨主机 PLC/BMS 仲裁。

## 9. Sustain-LC 实际 FMU 闭环

参考 [HPE Sustain-LC](https://github.com/HewlettPackard/sustain-lc) 本地仓库中的 LC_Frontier_5Cabinet_4_17_25.fmu。它是 FMI 2.0 Co-Simulation、固定五组共享一次侧模型，有 Linux x86-64/Windows 二进制，没有 macOS 二进制。当前加载真实动态库并执行 do_step，新动作会影响后续状态。

Apple Silicon 本机复用已有 sustain-lc:amd64 镜像（含 PyFMI、matplotlib）。--pull=never 不下载或构建镜像；换机需准备环境。原生 Linux x86-64 具备依赖可去掉 --docker。每策略独立进程/容器实例化，符合“一进程一次实例”声明；容器禁网，源码/FMU 只读挂载，结果写入本次目录。

### 9.1 控制范围与映射

Master 独占五组 FMU，现有 Engine、FlowPolicy、网关只控制第 1 组压差。第 2–5 组负荷、压差、供温固定；各组阀门、湿球温度、冷却塔目标固定。记录各组温度及设备功率；这不是多 CDU 协调优化。

shared_hydraulics 保持 true。专用入口注入 FmuLaboratoryGate，验证仿真适配器和 Master 所有权才许可实验；默认现场 ControlService 仍拒绝共享单域控制，不能靠现场 JSON 开关启用该例外。

下表 prefix 为 simulator[1].datacenter[1].computeBlock[1].cdu[1]。

| 标准量 | FMU 来源与转换 |
|---|---|
| 二次供回温 | prefix.summary.T_sec_s / T_sec_r，K |
| 二次质量流量 | prefix.summary.m_flow_sec，kg/s |
| 实际压差 | (prefix.summary.p_sec_s − prefix.summary.p_sec_r) / 1000，kPa |
| 第 1 组泵功率 | prefix.summary.W_flow_CDUP，W，归一为 W_e |
| 压差目标 | simulator_1_datacenter_1_computeBlock_1_cdu_1_sources_dp_nom_RL；软件 kPa 换算为原生 psi 输入 |
| 目标回读 | 同一外部输入；另检查到内部比较器的传递 |

**原始 27.5 对应约 189.606 kPa，不是 27.5 kPa。** 输入 XML 未标压力单位，内部 controls.gain.k=0.00014503773800721815 将 Pa 换 psi 后比较。适配器核对换算、别名、输出单位及传递路径，1 原始单位约为 6.89475728 kPa。

### 9.2 实验条件与限制

| 设置 | 本轮取值 |
|---|---|
| 共同预热 / 评估窗口 | 3600 s / 3600 s |
| 通信步长 / 控制周期 | 15 s / 60 s |
| 第 1 组各外部热源 | 每 600 s 为 60、75、50、65、80、60 kW |
| 其余四组各热源 | 固定 60 kW |
| 供温 / 支路阀门 | 28°C / 0.33、0.33、0.34 |
| 固定压差 / 目标范围 | 27.5 psi / 25–38 psi，均换算为软件 kPa |
| 最大单次变化 / 初始增益 | 3 kPa / 0.95 kg/s/√kPa |

这些是实验条件，不是 OEM 限值。ComputePowerBlade 是外部热源输入，尚未核对其总和到瞬时 CDU 液冷热负荷的关系。本次不把它当独立热负荷喂给算法，也不提供未来负荷，未验证预测前馈收益。

按真实模型时钟单调推进；非零 do_step 状态、时间不一致、非有限输出均停止，失败后禁止再写，不伪造时间戳。结束恢复固定压差，标记 experiment_fixed_hold，不表示 OEM 本地接管。

启动存在大温度瞬态，当前只评价预热后窗口，不验证启动安全。评估期间存在负绝对压力，最低约 −0.94 MPa；零点、初始化与物理一致性未解决。差压可检查控制响应，不能外推实机；冷却通道液温也不是芯片结温。

### 9.3 输出与历史数据

每策略输出 trace.csv、runtime.sqlite、commands.jsonl、fmu_writes.json、summary.json、scene.json、report.html。套件提供 manifest.json（源 FMU/代码哈希、条件）、接口证据、日志、PNG/SVG 和总报告。[最新 FMU 基准](../outputs/sustain-fmu-closed-loop-v2/index.html) 目录的 v2 表示实验修订，不是软件版本。

历史 CSV 回放不启动 FMU、不执行候选动作：

```bash
python3 tools/replay_sustain.py ../sustain_lc_validation/time_constant_trace.csv \
  --output outputs/my-replay-001
```

当前脚本已纠正压差单位，[修正结果](../outputs/sustain-replay-unit-corrected/replay_summary.json) 为有效参考。旧 outputs/sustain-replay 的压差候选不再作为接口证据。历史轨迹无实际压差观测，校准冻结；回温预测误差等于持值基线。

`../sustain_lc_validation/` 留存早期探测、供温阶跃/保持实验脚本、CSV、JSON，用于追溯，不代替本节流程。早期阶跃显示共享耦合/振荡，响应估计受 15 s 采样与 150 s 分块限制，不能从单工况推出所有 CDU 周期。历史生成器可重现当时文字，须结合本节单位和物理限制解读。

## 10. 已有验证结果与指标

下表来自保存的实验；此次文档整理未重跑控制实验、无新增实机结果。功能以 v0.4.0 代码为准，实验以各自条件和源哈希为准。

| 验证 | 已有结果 | 能证明什么 |
|---|---|---|
| 回归测试 | [53 项通过](../outputs/test-results-fmu.txt) | 当前覆盖的通信、网关、校准、配置和 FMU 接口行为，不是现场验收 |
| TCP 模拟器 | [4 周期、3 目标确认、4 心跳、退出本地接管](../outputs/wire-demo/wire-result.json) | 真 TCP 与模拟设备交互，无厂家硬件 |
| 合成液对液 | [1800 s 三策略](../outputs/no-hardware-l2l-20260914/comparison.json) | 在线对比固定算法泵电量低约 15.1%，最高回液高约 1.12 K；8 次增益更新 |
| 合成液对气 | [1800 s 三策略](../outputs/no-hardware-l2a-20260914/comparison.json) | 固定/在线泵电量相同；增益不参与直接流量目标；无排热风机/机房空调模型 |
| CSV 回放 | 441 记录、439 个 30 s 预测，MAE 约 0.0977 K | 等于持值基线，无动作，不能归因节能 |
| 实际 FMU | 三策略完成，各 240 评估采样；固定/在线各 58 目标确认，在线 3 次更新 | 在参考模型能闭环，未取得节能收益 |

### 10.1 实际 FMU 温度与电量

| 策略 | 第 1 组泵 kWh | 模型所列冷却 kWh | 第 1 组最高回液 °C |
|---|---|---|---|
| 固定压差基线 | 3.38058 | 126.03838 | 41.7986 |
| 固定参数算法 | 3.39166 | 126.04642 | 41.7349 |
| 在线校准算法 | 3.42317 | 126.07810 | 41.6984 |

按同一窗口通信步端点功率梯形积分。模型所列冷却包括 5 台 CDU 泵、热水环路泵、冷却塔环路泵和塔风机，不含 IT 或模型外设备，不是全 AIDC 电耗。在线泵耗增加、最高回温略降，控制成功和节能成功分别判断。

### 10.2 产品指标如何验收

| 指标 | 口径 | 当前缺口 |
|---|---|---|
| 通信执行 | 采集成功率、端到端/确认时延、拒绝/不确定次数、实际跟踪误差 | 有日志和测试，缺 OEM 台架/现场长期分布 |
| 热运行 | 关键测点峰值、越限时长、阶跃恢复；机柜需关键温度和最小流量 | 只覆盖模型/配置守护量，无芯片安全验证 |
| 校准 | 有效样本、冻结原因、保留误差、参数幅度、跨工况表现 | 有候选/晋升记录，缺独立现场数据 |
| 能效 | 同负荷、环境、边界、服务水平下积分电量，明确计量与对照 | 合成部分改善；本轮 FMU 无改善；无现场承诺 |
| 可靠性 | 失联接管、重复写、重启对账、磁盘容量、长期稳定性 | 部分故障自动测试；高可用、自动对账、容量治理未完成 |

不从厂家宣传复制节能率、毫秒响应或温控精度作为已达指标。现场目标由设备能力、传感器、工程限值及验证条件确定。

## 11. 逐文件代码职责

建议阅读 contracts → configuration → modbus → runtime → policy → engine → operations，再看仿真和测试。算法只生成标准请求，不直接写寄存器。

| 文件 | 职责与修改入口 |
|---|---|
| `lc_control/__init__.py` | 包说明，无运行循环 |
| `lc_control/__main__.py` | validate/demo/simulate/run/emulator/report、参数、退出码与 SIGTERM |
| `lc_control/contracts.py` | Reading、Snapshot、能力、请求/回执、适配器接口；区分目标与实测 |
| `lc_control/configuration.py` | 配置、资产、热汇、控制域、单位与工程校验 |
| `lc_control/adapters.py` | 静态 mock/故障开关，用于网关测试，不是热模型 |
| `lc_control/modbus.py` | TCP 报文、编码、点表、真实读写/交接 |
| `lc_control/runtime.py` | 命令网关、审计、幂等、保护、回读、暂停；动作必经入口 |
| `lc_control/engine.py` | tick、校准/决策/记录、模型恢复、锁、可注入网关工厂 |
| `lc_control/policy.py` | 热需求、预测校验、流量/压差目标、水力校准 |
| `lc_control/storage.py` | SQLite 事件读写，未来增加留存/备份/查询 |
| `lc_control/plant.py` | 合成热工对象、泵动态与电功率 |
| `lc_control/emulator.py` | 本机 Modbus 服务、模拟对象、独立看门狗，测试夹具 |
| `lc_control/operations.py` | 运行循环、三策略比较、域选择、退出处理 |
| `lc_control/report.py` | 保存记录生成会话离线 HTML，无实时写按钮 |
| `lc_control/sustain_fmu.py` | FMU Master、接口/单位、模型推进、适配与受限实验网关 |
| `tools/run_sustain_fmu.py` | 独立进程/Docker、预热扰动、三策略、邻机包络、电量积分 |
| `tools/sustain_fmu_report.py` | FMU 曲线与中文报告 |
| `tools/inspect_fmu.py` | XML 元数据检查，不加载动态库 |
| `tools/replay_sustain.py` | CSV 因果回放，不执行候选，纠正原始压差单位 |
| `tools/wire_demo.py` | 四周期真实 TCP 演示和退出检查 |
| `tools/build_manual.py` | 从本文生成 HTML 与根目录兼容入口，不读取设备 |
| `tools/build_portal.py` | 精简导航页，链接唯一手册与已有报告 |
| `tests/test_runtime.py` | 模式、边界、审计、回读、失效、回退 |
| `tests/test_product.py` | 两种控制模式、预测、校准/冻结、恢复、数值回归 |
| `tests/test_modbus.py` | TCP 功能码、编码、超时/丢包、只读、源停更、退出 |
| `tests/test_sustain_fmu.py` | 单位、实测/目标、时钟、失败、共享隔离和实验退出 |

| 配置/目录 | 用途 |
|---|---|
| `examples/liquid_to_liquid.json` / `examples/liquid_to_air.json` | 静态 mock，仍可 validate/demo，不能用于热工效果结论 |
| `examples/thermal_liquid_to_liquid.json` / `examples/thermal_liquid_to_air.json` | 动态合成对象，使用 simulate |
| `examples/modbus_emulator.json` / `examples/modbus_monitor_only.json` | 本机虚构设备可控/只读点表 |
| `examples/field_template.UNBOUND.json` | 未绑定现场模板，故意不能直接控制 |
| `examples/sustain_fmu_experiment.json` | 实验范围/扰动/参数，可用实验工具 --config 指定 |
| `deploy/lc-control-monitor.service` | 未安装/验收的 Linux 只读示例 |
| `pyproject.toml` / `.gitignore` | 包元信息、忽略规则，非完整发布流水线 |
| `outputs/` | 实验快照；第 10 节选择当前基准，不把目录名当软件版本 |
| `docs/assets/` | 产品示意图，不是已实现 UI 截图或验收证据 |

### 11.1 调用链与扩展原则

```text
CLI → load_scene / validate_scene → operations.run
  → 锁定设备端点和输出目录
  → Adapter + Engine(ControlService, FlowPolicy, Store)
  → 周期 tick
      读取 Snapshot → 保存 telemetry → heartbeat / 运行检查
      → observe / 校准记录 → propose / 标准请求
      → submit / 网关 → 先写审计 → 设备写入和回读
      → 保存 decision
  → finally: close / 尝试交还本地 → 关闭数据库 → 报告
```

新增协议实现 describe、read、write、心跳和释放语义，保留网关。新增模型/优化器通过标准观测/请求接入，不绕过审计，不修改保护限值。新增字段必须有执行语义和校验。

## 12. 数据、故障与恢复

SQLite 使用 WAL/FULL，事件含 session、telemetry、decision、calibration、model、fault、forecast_input_error。commands.jsonl 保存模式、dispatch_intent、回执、交接。写意图先落盘同步，再发设备命令；采样存储失败不静默丢弃。

| 状态/原因 | 含义与处理 |
|---|---|
| setpoint_confirmed | 目标回读一致，继续观察实际响应 |
| shadow | 未执行候选，不计算实际节能 |
| control_owner_mismatch / device_mode_mismatch | 模式/归属不允许，核对交接，不伪改枚举 |
| temperature_protection_not_verified | 缺所需防凝露证据/露点条件，维持温度写入禁用 |
| minimum_interval_not_met | 动作太密，等下一周期，不删除保护 |
| invalid_guard_observation / stale_source | 守护量/源刷新异常，检查采样链路 |
| uncertain / restart_pending_intent | 结果未知，禁止自动重发，核对设备并保留审计 |
| handoff_unconfirmed | 未确认本地接管，核对设备模式和看门狗 |
| exit_code=2 | 本次故障，查看 fault 和命令日志再决定恢复 |

同命令 ID 不重复写，冲突 ID 拒绝。未完成意图、不确定动作或损坏审计阻止重启自动控制。先核对设备目标/模式/接管，保存处置证据及原日志，确认交接后建新会话；**不要删审计解锁。** 没有自动对账/回滚向导，不应无条件重启为 control。

没有自动留存、备份、磁盘容量治理或高可用。长期边缘运行前需补齐；学习模型不替代设备保护。

## 13. 后续开发与文档维护

| 顺序 | 待完成交付 | 验证依据 |
|---|---|---|
| 1 | 首款真实 CDU 适配、写语义、属主、失联接管 | 型号/固件点表与台架/现场记录，先监测后闭环 |
| 2 | FMU 绝对压力、初始化、热源到液冷热负荷核对，扩大工况 | 扰动、参数失配、延迟/噪声、失败样本和温度/能量比较 |
| 3 | 独立负荷预测、电表与跨工况校准 | 时间/计量对齐，证明前馈和参数更新增益 |
| 4 | 接入向导、实时运维、配置发布、权限、通知、留存、恢复 | 长期运行与故障演练 |
| 5 | 多 CDU 协调、供温/冷源组合优化、其他协议 | 共享拓扑、能力和总冷却计量基础上的协调与能效验收 |

“以系统总能耗最低为目标，随负载与环境选择冷源组合及运行参数”仍是后续目标。当前是单域有界控制与增益校准，不是已完成的全站自适应节能。泄漏预测、冷却液健康、芯片模型、风液协同也未实现。

### 13.1 一个完整事实来源

完整内容只编辑本 Markdown，README 保留启动入口，门户只导航。更新后在项目目录执行：

```bash
python3 tools/build_manual.py
python3 tools/build_portal.py
```

前者生成同名 HTML，并更新根目录原“设计与验证方案.html”为跳转入口；后者生成 index.html。不要手改生成 HTML。新增功能同步第 2 节能力、第 3–4 节配置、第 11 节代码、第 13 节待办；新指标须有匹配证据才更新第 10 节。

此次合并早期定义、实施清单、重复厂家调研和分散设备/FMU/验证说明，删除已替代草案。过时根目录概念接口 JSON 与混合 Pod 示例由当前 examples 取代。用户标准 PDF、参考脚本、原始 CSV/JSON、图片与历史运行报告保留。历史实验是快照，不能冒充新版本结果；新实验写新目录并更新本文基准链接。
